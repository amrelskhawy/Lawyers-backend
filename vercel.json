{
    "version": 2,
    "builds": [
        {
            "src": "src/server.ts",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "dist/**",
                    "src/modules/**/*.ts",
                    "src/modules/**/*.swagger.ts",
                    "src/core/routes/**/*.ts",
                    "node_modules/swagger-ui-dist/**"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/server.ts"
        }
    ]
}